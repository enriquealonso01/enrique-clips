[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback so client-side routes (React Router) resolve on direct hit / refresh.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
